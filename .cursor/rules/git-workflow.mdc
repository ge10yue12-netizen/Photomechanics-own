---
description: 本项目 Git 自动提交与推送（Photomechanics-own）
alwaysApply: true
---

# Git 工作流 — Photomechanics-own

## 仓库信息

- 远程：`https://github.com/ge10yue12-netizen/Photomechanics-own.git`
- 默认分支：`master`
- 作者：`ge10yue12-netizen <ge10yue12-netizen@users.noreply.github.com>`
- 代理（推送用）：`http://127.0.0.1:7890`（Clash；端口变更时更新 `.git/config`）

## 何时执行 Git

在完成**用户要求的代码修改**后（非纯问答、非只读审查），在回复用户前：

1. `git status` / `git diff` — 确认改动范围
2. `git add` — 只加入与本次任务相关的文件，不提交 `.env`、密钥、本地构建产物
3. `git commit -m "..."` — 中文或英文均可，说明「为什么改」，1～2 句
4. `git push origin master` — 推送到 GitHub

## 不做的事

- 用户仅提问、未要求改代码 → 不 commit
- 用户明确说「先别提交 / 别 push」→ 遵守
- 不用 `--force`、不改写历史，除非用户明确要求
- 不提交含密码、Token 的文件

## 推送失败

- 网络/代理问题：本地 commit 保留，告知用户开 Clash 后 `git push`
- 认证失败：提示用 GitHub Token，不要用登录密码

## 换新项目时

用户说明新仓库 URL 后：初始化/绑定 remote、更新本 rule 中的仓库信息，或新建对应项目的 `git-workflow.mdc`。
