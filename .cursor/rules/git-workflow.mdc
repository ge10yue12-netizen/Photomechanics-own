---
description: 本项目 Git 提交与推送规范（Photomechanics-own）
alwaysApply: true
---

# Git 工作流 — Photomechanics-own

## 仓库信息

- 远程：`https://github.com/ge10yue12-netizen/Photomechanics-own.git`
- 默认分支：`master`
- 作者：`ge10yue12-netizen <ge10yue12-netizen@users.noreply.github.com>`

## 网络与代理（不绑定固定端口）

- Git **不要求**在规则或仓库里写死 `http.proxy` / `https.proxy` 端口
- **推荐**：Clash 开 TUN / 系统代理，Git 不设 proxy，改代理端口后仍可直接 push/pull
- 若本机 `.git/config` 或全局仍留有旧 proxy（如 `127.0.0.1:7890`），端口变更会导致失败；可在本机执行 unset，勿由 Agent 修改 git config：

```powershell
git config --unset http.proxy
git config --unset https.proxy
git config --global --unset http.proxy
git config --global --unset https.proxy
```

- 仅在使用 HTTP 代理模式、且不用 TUN 时，才需用户自行按当前 Clash 端口配置 Git proxy

## 何时执行 Git

- **仅当用户明确要求** `commit` / `push` / 「提交」时才执行
- 完成代码修改后：**默认不自动 commit/push**
- 用户仅提问、审查、分析 → 不 commit
- 用户说「先别提交 / 别 push」→ 必须遵守

## 提交步骤（用户要求提交时）

1. `git status` / `git diff` — 确认范围
2. `git add` — 仅本次相关文件；不含 `.env`、密钥、构建产物、本地测试大图
3. `git commit -m "..."` — 1～2 句，说明「为什么改」
4. 用户要求 push 时：`git push origin master`

## 禁止

- `--force`、改写历史（除非用户明确要求）
- 未询问用户而主动 commit/push
- 提交含密码、Token 的文件
- Agent 修改 `git config`（含 proxy、user 等）

## 推送失败

- 网络不可达：保留本地 commit，提示检查 Clash TUN/系统代理或网络后重试 `git push`
- 若报错 `127.0.0.1:xxxx` 连接失败：多为 Git 内残留 proxy，提示用户 unset 或对齐当前代理端口
- 认证失败：提示 GitHub Token，勿用登录密码

## 换新项目时

用户给出新仓库 URL 后：绑定 remote 并更新本 rule 中的仓库信息。
