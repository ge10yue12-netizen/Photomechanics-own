---
description: 项目 Agent 强制总则（每次任务必读必遵）
alwaysApply: true
---

# Agent 强制总则 — QtProject_1

## 任务开始时（强制）

1. 阅读并遵守本目录下全部 `alwaysApply: true` 的规则（含 `skills-workflow.mdc`）
2. 按 `skills-workflow.mdc` 判断并 Read、执行对应 Skill
3. 用户可见回复**首句**必须是：**你好，阿跃**
4. 查库/API/框架文档时优先用 **Context7**（`user-context7` MCP），勿凭记忆臆测
5. 回复末尾注明：**本次执行 Skill：\<名称\>**（见 `skills-workflow.mdc`）

## 改代码前（强制）

1. **先理解**：现有目录、命名、调用关系；用到的类/函数/字段须项目中真实存在
2. **先方案、后动手**：用中文给出需求理解、修改思路、影响范围与风险；**此轮回复不得改任何代码/配置/规则文件**
3. **必须等你确认**：仅当用户在后续消息中明确回复 **「确认修改」** 后，下一轮才可执行代码修改；其他表述（如「可以」「开始吧」「做吧」）**不算**确认，仍只给方案
4. 以下情况**仍须先方案、等确认**，不得直接改代码：
   - 用户说「优化 / 实现 / 可以做 / 开始吧」但未看过具体方案
   - 用户补充需求、调整方向
   - 上一轮只做了分析或审查
5. 确认后执行时：**最小 diff**，不改无关文件；不堆新类/新文件除非方案中已说明且你已同意

## 禁止在未确认时执行的操作

- 编辑、新建、删除源码或项目配置文件
- `git commit` / `git push`（另见 `git-workflow.mdc`）
- 用户只问「能不能 / 怎么做 / 在哪里」→ 只答不改

## 代码与注释

- **最小改动**、匹配项目风格；不重写已可用逻辑；不为优化而加抽象/依赖
- **新增**变量、函数、类、配置、分支/循环/异常处理 → 须加 `//` 注释，说明用途或约束
- 注释**正式、简洁**，禁止口语（如「就这三样」「不被移走」「打日志」「误以为」）
- 不写无意义注释；修改逻辑时同步更新过期注释
- 代码格式统一；命名表意清楚；不吞错误

## 回复结构（涉及代码改动时）

1. 需求理解 → 2. 思路 → 3. 按文件说明改动 → 4. 总结（改了什么 / 为什么 / 风险 / 如何验证）
- 未实际运行测试或编译时，**不得声称已测试**
- 用户日志/UI 文案用中文；代码标识符与现有项目一致

## Git

- **仅用户明确要求**时才 `git commit` / `git push`（细则见 `git-workflow.mdc`）
- 不 `--force`；不提交密钥、`.env`、构建产物

## 环境

- 须用工具实际执行命令与读文件，不得空口推断
