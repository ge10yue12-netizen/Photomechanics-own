---
description: 项目 Agent 强制总则（每次任务必读必遵）
alwaysApply: true
---

# Agent 强制总则 — QtProject_1

## 任务开始时（强制）

1. 阅读并遵守本目录下全部 `alwaysApply: true` 的规则（含 `skills-workflow.mdc`）
2. 编辑源码前须遵守 **`~/.cursor/rules/code-comments.mdc`**（全局）；本项目补充见 `.cursor/rules/code-comments.mdc`、`cpp-qt-standards.mdc`
3. 按 `skills-workflow.mdc` 判断并 Read、执行对应 Skill
4. 用户可见回复**首句**必须是：**你好，阿跃**
5. 查库/API/框架文档时优先用 **Context7**（`user-context7` MCP），勿凭记忆臆测
6. 回复末尾注明：**本次执行 Skill：\<名称\>**（见 `skills-workflow.mdc`）

## 改代码前（强制）

1. **先理解**：现有目录、命名、调用关系；用到的类/函数/字段须项目中真实存在
2. **先方案、后动手**：用中文给出需求理解、修改思路、影响范围与风险；**此轮回复不得改任何代码/配置/规则文件**
3. **必须等你确认**：仅当用户在后续消息中明确回复 **「确认修改」** 后，下一轮才可执行代码修改；其他表述（如「可以」「开始吧」「做吧」）**不算**确认，仍只给方案
4. 以下情况**仍须先方案、等确认**，不得直接改代码：
   - 用户说「优化 / 实现 / 可以做 / 开始吧」但未看过具体方案
   - 用户补充需求、调整方向
   - 上一轮只做了分析或审查
5. 确认后执行时：**最小 diff**，不改无关文件；不堆新类/新文件除非方案中已说明且你已同意

## 禁止在未确认时执行的操作

- 编辑、新建、删除源码或项目配置文件
- `git commit` / `git push`（另见 `git-workflow.mdc`）
- 用户只问「能不能 / 怎么做 / 在哪里」→ 只答不改

## 代码与注释

- **最小改动**、匹配项目风格；不重写已可用逻辑；不为优化而加抽象/依赖
- **新增**变量、函数、类、配置、分支/循环/异常处理 → 须加注释（细则见 **`~/.cursor/rules/code-comments.mdc`**）
- 注释须**严谨、书面、简洁**：说明用途及约束/边界/风险；**禁止口语**（如「打日志」「就这三样」「弄一下」「误以为」）
- 不写无意义或与代码重复的注释；修改逻辑时**同步更新**过期注释
- 代码格式统一；命名表意清楚；不吞错误

## 改代码后（强制）

完成源码或模块行为改动后，**同一轮任务内**同步根目录 `README.md`，使摘要与当前项目一致。

**须更新 README 的情况**（改动涉及任一项）：

- 模块职责、架构或数据流
- 对外入口函数、用户操作流程
- 目录结构、编译/运行方式
- 当前版本行为说明（「版本」表）

**README 中常改章节**：「一分钟了解」「架构一览」「快速操作 → 代码入口」「目录结构」「版本」。

**可不更新 README**：纯内部重构、重命名、注释或不影响对外行为的微小修复；若不确定，在「版本」表补一行摘要。

**与 DEVELOPER_GUIDE 分工**：README 为入口摘要，须与代码一致；详细时序与排错仍见 `docs/DEVELOPER_GUIDE.md`。架构级大改时在回复中说明是否需同步 DEVELOPER_GUIDE，不擅自大段重写除非用户要求。

## 回复结构（涉及代码改动时）

1. 需求理解 → 2. 思路 → 3. 按文件说明改动 → 4. 总结（改了什么 / 为什么 / 风险 / 如何验证）
- 总结须含一行：**README：已更新 / 无需更新（原因）**
- 未实际运行测试或编译时，**不得声称已测试**
- 用户日志/UI 文案用中文；代码标识符与现有项目一致

## Git

- **仅用户明确要求**时才 `git commit` / `git push`（细则见 `git-workflow.mdc`）
- 不 `--force`；不提交密钥、`.env`、构建产物

## 环境

- 须用工具实际执行命令与读文件，不得空口推断
