---
description: Skill 强制读取与执行规范（每次任务必读）
alwaysApply: true
---

# Skill 强制规范 — QtProject_1

## 任务开始时

根据用户请求判断是否命中 Skill；命中则 **第一步 Read 对应 SKILL.md 全文**，再分析或改码，禁止跳过。

| Skill | 路径 | 触发条件 |
|-------|------|----------|
| code-reviewer | `~/.cursor/skills/code-reviewer/SKILL.md`（本项目：`.cursor/skills/code-reviewer/SKILL.md`） | 检查 / review / 找 bug / 优化代码 / 提交前检查 |
| debug-helper | `.cursor/skills/debug-helper/SKILL.md` | 编译失败、运行报错、启动失败、链接错误 |
| test-writer | `.cursor/skills/test-writer/SKILL.md` | 写测试、补测试、单元测试 / 集成测试 |
| Context7 | MCP `user-context7` | 查 Qt、Pylon、第三方库 API 与用法 |

## 执行要求

1. 使用 Read 工具读取 SKILL.md，不得仅凭记忆或摘要执行
2. 严格遵循 Skill 内「工作流程」与「输出格式」
3. 用户可见回复末尾注明：**本次执行 Skill：\<名称\>**（未命中则写「无」）

## 与本仓库改码流程衔接

- 用户回复「确认修改」并完成代码改动后，涉及功能或 bug 修复时，**默认追加 code-reviewer 自检**（按 SKILL 输出格式给出审查摘要，**含注释规范检查**），除非用户说「不要 review」
- 改码或运行过程中出现编译 / 运行错误，**切换 debug-helper**，按该 Skill 四段格式输出
