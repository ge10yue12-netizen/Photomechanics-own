---
description: C++/Qt 编码与存图约定
globs: "**/*.{cpp,h,hpp}"
alwaysApply: false
---

# C++/Qt 约定 — QtProject_1

注释遵 `~/.cursor/rules/code-comments.mdc`。

## 模块边界

| 模块 | 职责 |
|------|------|
| `SavePathHelper` | 路径与 Pic/CAMERA 计数 |
| `ImageSaveThread` | `m_taskQueue` 后台写 BMP |
| `QtProject_1::enqueueCurrentFrame` | 取帧、算路径、入队 |

存图容器 = `ImageSaveThread` 内 `QQueue<SaveTask> m_taskQueue`。不另建 `BoundedQueue`、`BufferPool` 等封装，除非用户确认架构变更。

## 入队流程

```
copyLatestImage（Grayscale8 深拷贝）→ SaveTask move 入队 → m_taskQueue → run() → writeBmpFile
```

队列满时 `trySubmit` 返回 false，触发 `queueFull`；禁止静默丢帧。`nextFilePath` 预分配路径，Pic 序号在 `onFileSaved` 成功后递增。

## 注释示例

```cpp
// 非阻塞入队；队列已满时返回 false，且不移动 task
bool trySubmit(SaveTask &task);

QQueue<SaveTask> m_taskQueue; // 存图任务队列，上限 48，由 ImageSaveThread::run 消费
```

## 格式禁止

整文件「每行后空一行」的异常排版。
