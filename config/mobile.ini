[mobile]
; HTTP 监听端口（绑定 0.0.0.0；二维码 URL 使用弹窗所选本机 IP）
port=8080
; 会话 token 有效期（秒）
token_lifetime_sec=600

[preview]
; 手机预览 JPEG 缩放上限（像素）
max_width=480
max_height=360
; JPEG 质量 30–90，越低体积越小、延迟越低
jpeg_quality=55
; 手机端预览轮询间隔（毫秒）；采用串行加载，避免请求堆积
poll_ms=120
; 手机端状态轮询间隔（毫秒）
status_poll_ms=800
