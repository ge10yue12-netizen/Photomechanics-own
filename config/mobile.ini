[mobile]
; HTTP 监听端口（绑定 0.0.0.0；二维码 URL 使用弹窗所选本机 IP）
port=8080
; 会话 token 有效期（秒）
token_lifetime_sec=600

[preview]
; 手机预览 JPEG 缩放上限（像素）；略降可减传输延迟
max_width=400
max_height=300
; JPEG 质量 30–90，越低体积越小、跟手越快
jpeg_quality=48
; 静止画面 HEAD 探测间隔（毫秒）；有新区块时 16ms 追帧
poll_ms=80
; 手机端状态轮询间隔（毫秒）
status_poll_ms=800
